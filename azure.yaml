# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: cosmosdb-movie-ai
metadata:
  template: cosmosdb-movie-ai@0.0.1-beta
services:
  api:
    project: ./
    host: containerapp
    language: ts
infra:
  provider: bicep
  path: infra
